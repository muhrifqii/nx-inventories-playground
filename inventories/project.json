{
  "name": "inventories",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "inventories/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/inventories"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "inventories:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "inventories:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "inventories:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "inventories:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "inventories:build:development"
        },
        "production": {
          "buildTarget": "inventories:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../coverage/inventories"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    }
  },
  "tags": []
}
